# Phase 3 完成报告：截图输入 + 多解法结构 + 排版升级

日期：2026-08-17

## 交付结果

- 引擎已支持 v2 多解法内容包：每个 approach 使用独立的轨迹、播放器和代码标签页，并按 `:::viz` 指令就地插入正文。
- 安全 Markdown 现支持固定语义的 `:::insight` 与 `:::pitfall`；未知指令仍会转义，不会执行 HTML。
- 新增 LC 300 正式页面：官方动态规划和社区高赞贪心 + 二分各有一套独立播放器。
- 新增 `_qc-checklist.md`，未进入索引或页面渲染。

## 输入与来源决策

- 原题：按用户上传截图逐字转录了题号、题干、三个示例和两条约束。
- 官方题解：LeetCode Premium 页面无法直接读取；用户改为完整粘贴官方 editorial 文本。页面仅转述其中的 DP 推理和复杂度，不复刻长段落。
- 社区题解：采用用户提供的 [高赞解法](https://leetcode.com/problems/longest-increasing-subsequence/solutions/1326308/cpython-dp-binary-search-bit-segment-tre-wc3w) 的“贪心 + 二分”思路；展示代码与轨迹均由本项目独立实现，没有复制社区代码。
- 收录决策：本页选定官方 `O(n^2)` DP 作为官方路线；社区的 `O(n log n)` 贪心 + 二分提供了更优复杂度与不同的状态表达，因此单独收录、单独可视化。

## 浏览器验收（本地静态服务器）

- [x] 页面加载无 console error / warning。
- [x] 原题转录显示完整；`_qc-checklist.md` 未出现在页面。
- [x] 官方 DP 播放器出现在官方讲解下方，初始帧显示原数组与 DP 行；依赖状态和当前格清晰可见。
- [x] 社区贪心 + 二分播放器出现在社区讲解下方，显示实时 `tails` 与 `slot`。
- [x] 点击官方“下一步”后官方到 `#1`、社区仍为 `#0`；官方重置后社区状态不受影响。
- [x] 社区滑杆跳转到 `#5`，正确显示处理 `nums[4] = 3` 后的 `tails = [2, 3]`。
- [x] 官方“播放”自动到最后 `#7` 后停止；社区仍保留在 `#5`。
- [x] 社区“播放”自动到最后 `#8` 后停止；官方仍保留在 `#7`。
- [x] 末帧“下一步”禁用，首帧“上一步”禁用。
- [x] 社区 C++ 标签可切换并显示 Prism 高亮。
- [x] 两个 `:::insight` 和一个 `:::pitfall` 按固定语义样式显示。

## 自动验证

- [x] 两个 `generate_trace.py --check` 均通过，轨迹均来自真实算法执行。
- [x] `node --test`：25/25 通过。
- [x] `node scripts/build-index.mjs`：索引成功生成 5 道题。
- [x] 展示的 Python 与 C++：官方 DP、社区贪心 + 二分均在示例输入上运行得到 `4`。
- [x] `git diff --check` 通过。

## 发现并修复的问题

安全 Markdown 子集不支持表格，最初的复杂度 Markdown 表格被渲染为一段文本。已改为三个紧凑项目，保留同等比较信息并符合安全子集。

## 设计结论

现有组件足以覆盖 LC 300：DP 用 `dp-grid`，贪心二分用 `array-pointers` 加 `note` 表达 `tails` 的非索引语义。无需为本题新增组件。
