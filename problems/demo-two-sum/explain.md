# 有序数组上的 Two Sum

数组已经升序排列。把 `left` 放在开头、`right` 放在结尾：

- 和太大时，右端左移；
- 和太小时，左端右移；
- 相等时得到答案。

## 为什么不会漏解？

当当前和太大，保持 `right` 不动只会让 `left` 右移后的和更大，因此只能缩小 `right`。和太小时同理。每一步都会排除一个不可能参与答案的位置。

时间复杂度是 **O(n)**，额外空间是 **O(1)**。
