# LC 300 生成自检

- [x] 原题标题、描述、三个示例、约束与 Follow up 已按用户提供的原题截图逐项转录。
- [x] 已保留用户提供的官方三种方案：动态规划、候选序列 + 线性扫描、候选序列 + 二分查找。
- [x] 已保留用户提供的社区五种材料：记忆化搜索、递推、贪心 + 二分的额外空间写法、原地写法，以及返回一条具体 LIS 的扩展写法。
- [x] 八个方案均有独立 `generate_trace.py`、`trace.json`、`meta.json` 代码与 `:::viz` 播放器；没有因等价、较慢或实现变体而降级为纯文字。
- [x] DP / 记忆化方案复用 `dp-grid`；候选结尾、原地修改及回溯方案复用 `array-pointers`，未擅自扩展引擎。
- [x] “题目拆解与通用切入点”覆盖判题契约、决定性约束、序列 DP 的状态切入和状态支配的压缩入口，直接回应“没有思路”。
- [x] 每个 trace 由真实算法执行生成；`--check`、展示 Python/C++ 可执行性与索引构建均已通过。
- [x] 浏览器实测：页面加载 8 个独立播放器；先操作第一个、再操作最后一个时仅对应实例步进，控制台 error/warning 为 0。
- [x] `:::insight` / `:::pitfall` 使用安全语义块；本文件不进入页面渲染或索引。
- [x] 正文按零基础、ADHD 友好的微步结构重写：首次术语有日常语言解释；每个方案都有局部例子、可跳过的“5 秒想一想”、紧跟的“直接看答案”和一条“现在只需要记住”。
- [x] 在 `http://localhost:8005/` 复测：显示 8 个播放器；分别点击第一个和最后一个后恰好两者步进，控制台 error/warning 为 0。
- [x] 已按“前提链与因果连续性”复查：知识地图明确区分算法思想与数组容器；官方 DP 首次出现 `dp[i]` 前，先给出状态问题、表格用途、`nums[2] = 2` 的基例和 `nums[5] = 7` 的转移来源。
- [x] 在当前工作树的 `http://localhost:8006/` 真实浏览器复测代码行高亮：官方 DP 用 next/previous 在初始化 `dp = [1] * len(nums)` 与转移 `dp[i] = max(...)` 间切换，slider 跳到第 7 帧仍定位转移行，切换 C++ 后定位 `vector<int> dp(...)`；官方二分方案依次看到 `sub.append(num)`、`sub[slot] = num`，previous 回到追加行。控制台 error/warning 为 0。
