# LC 80 生成自检

- [x] 原题标题、描述、Custom Judge、两个示例与三条约束已按本轮用户提供的原题截图逐字转录。
- [x] 正文使用“题目拆解与通用切入点”而非“暴力解法推导”：先解释判题契约、决定性约束、相关概念与通用切入法，再进入官方方案；本地浏览器确认章节顺序和 Markdown 渲染正常，console 无 error/warning。
- [x] 官方来源使用本轮用户粘贴的官方题解；其中 Approach 1“删除多余重复项”和 Approach 2“覆盖多余重复项”算法不同，均独立收录、讲解和可视化；正文只转述其推理与复杂度。
- [x] 社区来源使用本轮用户粘贴的高赞题解；不复制社区代码，而是独立实现同一栈式思路，并完整保留讲解、播放器与 Python/C++。
- [x] 官方覆盖写入与社区栈式 `write - 2` 算法等价、同为 `O(n)` / `O(1)`，但遵循“不省略任何提供答案”的新 Skill 规则，仍各自保留完整方案包。
- [x] `official-pop-delete`、`official-overwrite` 与 `community-stack-write-two-back` 均由同目录 `generate_trace.py` 真实生成，并以 `--check` 验证。
- [x] 三个方案的 Python 与 C++ 展示代码与各自生成器使用相同算法；测试实际执行六段代码，均验证结果为 `[0,0,1,1,2,3,3]`。
- [x] 三个 `:::viz` 分别引用三个方案。本地浏览器显示“3 种解法”、三个播放器和代码；初始为 `#0 / #0 / #0`，只点击社区播放器下一步后为 `#0 / #0 / #1`，证明互不影响；console 无 error/warning。自动化 Browser 对原生 range 输入未派发拖动事件；该控件实现未在本次改动中变更，用户此前已确认滑杆正常。
- [x] `:::insight` / `:::pitfall` 只使用安全语义块；本文件不被渲染管线或索引读取。
